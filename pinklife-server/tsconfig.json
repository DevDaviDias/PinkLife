{
  "compilerOptions": {
    /* Layout do Projeto */
    "rootDir": "./",             // Onde o TS começa a procurar (pode mudar para ./src depois)
    "outDir": "./dist",          // Onde o código compilado vai morar

    /* Configurações de Ambiente */
    "module": "CommonJS",        // Padrão para Node.js/Express
    "target": "ES2020",          // Versão moderna do JS
    "lib": ["ES2020"],
    "esModuleInterop": true,     // Permite importar pacotes JS antigos (como bcrypt) sem erros
    "forceConsistentCasingInFileNames": true,

    /* Outputs */
    "sourceMap": true,
    "declaration": true,

    /* Strictness */
    "strict": true,
    "skipLibCheck": true,        // Ignora erros dentro da node_modules (essencial!)

    /* Removendo o que é específico de Frontend/React */
    "jsx": "preserve",           // O backend não renderiza JSX
    "noImplicitAny": false       // Deixamos false por enquanto para não "travar" sua refatoração
  },
  "include": ["**/*.ts"],        // Diz ao TS para olhar todos os arquivos .ts
  "exclude": ["node_modules", "dist"]
}